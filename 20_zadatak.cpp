#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	int inputNumber = 1;

	while (inputNumber != 0)
	{	
		int count, suma = 0;
		for (count = 1; count >= 1; count++)
		{
			scanf("%d", &inputNumber);
			suma += inputNumber;
		}
		

		
	}



	return 0;
}

